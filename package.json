{
  "name": "instagram-mini",
  "private": true,
  "scripts": {
    "start": "meteor run",
    "build": "meteor npm install && meteor npm install bcrypt"
  },
  "dependencies": {
    "babel-runtime": "^6.22.0",
    "bcrypt": "^1.0.2",
    "meteor-node-stubs": "~0.2.0"
  }
}
